Project Assumptions

In this private school, students enroll in up to 3 subjects per semester and have a total of 3 absences available per class
Each subject is taught daily in 3-hours classes

*NOTE*
ExportDatabaseScript contains the generated script of the whole database
TableCreate.sql, InsertData.sql and SQLQueries contain the queries i used to create the database
